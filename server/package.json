{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for Moder Dy app. Connects display and control.",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "dstart": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stuart Thomson",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "ts-node": "^8.3.0",
    "ws": "^7.4.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.10",
    "@types/ws": "^6.0.1",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}
